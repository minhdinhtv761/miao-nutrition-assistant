import { createAction } from 'redux-actions';

export const showEdittingUser = createAction(
    "SHOW_EDITING_USER"
  );
  
  export const hideEdittingUser = createAction(
   "HIDE_EDITTING_USER"
  );
  